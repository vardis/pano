The example script is also contained within the multifile so to view it you have first to extract the contents with: 
	multify -x -f resources.mf

To just view the contents of the archive: 
	multify -tv -f resources.mf
